<Window x:Class="GrassiBoard.Views.PadEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Sound Pad" Width="540" Height="430" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource PageBrush}"
        Foreground="{DynamicResource TextBrush}" ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="EDIT SOUND PAD" Style="{StaticResource EyebrowTextStyle}" />
            <TextBlock Text="Pad details" FontSize="23" FontWeight="SemiBold" Margin="0,5,0,0" />
        </StackPanel>
        <StackPanel Grid.Row="2">
            <TextBlock Text="TITLE" Style="{StaticResource EyebrowTextStyle}" />
            <TextBox x:Name="TitleBox" Margin="0,7,0,0" />
        </StackPanel>
        <StackPanel Grid.Row="4">
            <TextBlock Text="AUDIO FILE" Style="{StaticResource EyebrowTextStyle}" />
            <Grid Margin="0,7,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="PathBox" IsReadOnly="True" />
                <Button Grid.Column="2" Content="Browse" Style="{StaticResource SecondaryButtonStyle}"
                        Click="Browse_Click" />
            </Grid>
        </StackPanel>
        <StackPanel Grid.Row="6">
            <Grid>
                <TextBlock Text="VOLUME" Style="{StaticResource EyebrowTextStyle}" />
                <TextBlock x:Name="VolumeLabel" HorizontalAlignment="Right" FontFamily="{StaticResource MonoFont}" />
            </Grid>
            <Slider x:Name="VolumeSlider" Minimum="0" Maximum="1" Margin="0,8,0,0"
                    ValueChanged="VolumeSlider_ValueChanged" />
        </StackPanel>
        <StackPanel Grid.Row="8" Orientation="Horizontal">
            <CheckBox x:Name="LoopCheck" Content="Loop until stopped" Margin="0,0,28,0" />
            <CheckBox x:Name="RestartCheck" Content="Restart when pressed again" />
        </StackPanel>
        <StackPanel Grid.Row="10" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource SecondaryButtonStyle}" Click="Cancel_Click" Margin="0,0,10,0" />
            <Button Content="Save Pad" Click="Save_Click" />
        </StackPanel>
    </Grid>
</Window>
