namespace GrassiBoard.Models.Looper;

internal enum LooperLayerRecordMode
{
    OneCycle = 0,
    LoopReplace = 1,
    Overdub = 2
}

internal enum LooperLayerRecordState
{
    Idle = 0,
    Armed = 1,
    Recording = 2
}
